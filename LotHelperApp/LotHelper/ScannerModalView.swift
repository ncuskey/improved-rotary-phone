import SwiftUI

// NOTE: ScannerModalView is defined in StartScreenView.swift under conditional compilation
// for platforms with and without UIKit. This file intentionally contains no duplicate
// definition to avoid "Invalid redeclaration of 'ScannerModalView'" build errors.
